"""Analysis agents.

`base.py` holds the agent framework + shared helpers; `seo_agents.py` and
`geo_agents.py` hold the category agents themselves. The orchestrator imports
the agent classes directly and runs them concurrently.
"""
